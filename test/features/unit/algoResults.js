'use strict';

// const algoResults = require('../../../lib/algoResults');

describe('Algo Results', function () {

    it('Should convert tsv to json', () => {
		// TOOD: Algo tests
		// const dataPath = `${__dirname}/../../fixtures`;
		// algoResults.getResults({ lang: 'ar', wiki: 'wikipedia' }, {}, dataPath).then((r) => {
		// });
    });

});
